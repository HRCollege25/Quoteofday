document.addEventListener('DOMContentLoaded', (event) => { console.log('DOM fully loaded and parsed'); async function getquote(url) { const response = await fetch(url); const data = await response.json(); console.log('API Response:', data); const quoteElement = document.getElementById('quote'); const authorElement = document.getElementById('author'); console.log('Quote Element:', quoteElement); console.log('Author Element:', authorElement); if (quoteElement && authorElement) { quoteElement.innerHTML = data.quote ? data.quote : 'No quote found'; authorElement.innerHTML = data.author ? data.author : 'Unknown author'; } else { console.error('Elements with ID "quote" or "author" not found in the DOM.'); } } // Example usage getquote('https://api.example.com/quote'); });